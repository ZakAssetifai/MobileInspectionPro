<?xml version="1.0" encoding="utf-8"?>
<!--
  ArcGIS Maps SDK serves the MMPK + tile cache from 127.0.0.1, but Android
  API 28+ blocks cleartext (HTTP) by default. This config keeps cleartext
  blocked for the open internet (production-safe) and whitelists loopback.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
